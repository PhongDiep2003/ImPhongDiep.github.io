# Update and Deploy New Changes
   - Build the project
     ```
     npm run build
     ```
   - Test new changes in local dev
     ```
     firebase serve
     ```
   - Deploy changes
     ```
     firebase deploy
     ```
